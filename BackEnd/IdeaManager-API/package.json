{
  "name": "ideamanager-api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev:server": "ts-node-dev --inspect --transpile-only --ignore node_modules src/server.ts",
    "run:dev": "node build/server.js",
    "dev": "yarn build && yarn run:dev",
    "clean": "rm -r build"
  },
  "devDependencies": {
    "@types/date-fns": "^2.6.0",
    "@types/typescript": "^2.0.0",
    "@types/uuidv4": "^5.0.0",
    "ts-node-dev": "^1.0.0-pre.61",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "@types/express": "^4.17.7",
    "date-fns": "^2.16.0",
    "express": "^4.17.1",
    "uuidv4": "^6.2.3"
  }
}
